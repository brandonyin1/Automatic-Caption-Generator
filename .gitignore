# Debug exports contain full transcripts/captions from actual work sessions,
# not source code - generated output, not something to version.
Debug/

# Contains a hardcoded API key - do not commit.
transcribe_all.py

# OS junk
Thumbs.db
.DS_Store

# Electron build artifacts - installed/built locally, not versioned
electron/node_modules/
electron/dist/

# Root build tooling (build.js) currently has zero dependencies, but keep
# this ignored in case that changes later.
/node_modules/
